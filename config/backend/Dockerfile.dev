FROM maven:3.9-eclipse-temurin-21
WORKDIR /app

# Codigo e cache do Maven vem de bind mounts (docker-compose.override.yml),
# por isso a imagem nao copia src nem pre-baixa dependencias.

EXPOSE 8080
CMD ["mvn", "spring-boot:run"]
